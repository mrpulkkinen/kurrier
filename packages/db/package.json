{
	"name": "@kurrier/db",
	"private": true,
	"version": "1.0.6",
	"description": "",
	"types": "src/index.ts",
	"main": "src/index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"db:generate": "dotenvx run -f ../../.env -- npx drizzle-kit generate",
		"db:migrate": "dotenvx run -f ../../.env -- npx drizzle-kit migrate",
		"db:push": "dotenvx run -f ../../.env -- npx drizzle-kit push"
	},
	"keywords": [],
	"author": "Krishna Rokhale",
	"license": "ISC",
	"dependencies": {
		"jwt-decode": "^4.0.0"
	}
}
